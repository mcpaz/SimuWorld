Paisano:

	//solo tendra un metod que sera sulfatar.
	// proporcionara a las clases de de cepa y hongo 
	// si tiene o no hongo
	boolean getSulfatar();


Mundo:

	Atributos:

		int temperatura;
		float lluvia; //cantidad de lluvia  que cae 
		float humedad; 
		Strig tipoTierra; // tipo de tierra que es determiante en la produccion y calidad del vino
						  // por si queremos meter tambien este tipo de variable.
		int phTierra;
		boolean sulfato;//guarda si el paisano ha sulfatado o no.
		float velocidadViento;//puede influir en la expansion del hongo


	Metodos

		void leerFicheroPArametros();// metodo que lee de un fichero los datos para coger 
									 //los valores de lso diferentes atributos.

		itn g//etTemperatura();
		float getLluvia();
		float humedad();
		String getTipoTierra();



		float calcularProbabildadHongo(int crecimiento); //calcula la probabilidad de que una cepa 
												 //tenga o no un hongo(enfermedad), se calcula 
												 // por el parametro de crecimiento  del hongo
												 // que se obtiene de calcularCrecimientoHon();


		boolean infectarCepas(floa probabilidadHongo); // cuando la cepa pregrunte si tiene hongo o no
													   // este metodo decidira de forma aleatoria, teniendo
													   // en cuenta la probabilidad de tener hongo calcula antes, 
													   // si tiene o no.


	    boolean getSulfato(); // metodo que devuelve si las cepas esta o no sulfatadas
	    				   // lo que influira luego en la clases de de cepa y hongo(enfermedad)


	    float creceHongo();



Cepa:


	Atributos:
		//son atributos que comparte con el mundo
		int temperatura;
		float lluvia; //cantidad de lluvia  que cae 
		float humedad; 
		Strig tipoTierra; // tipo de tierra que es determiante en la produccion y calidad del vino
						  // por si queremos meter tambien este tipo de variable.
		int phTierra;
		boolean sulfato;//guarda si el paisano ha sulfatado o no.

		//atributos propios de cepa.

		int numerohojas;
		float tamanoHojas;
		int numRacimos;
		int numUvas;
		float pesoUva;

		boolean hongo; //guarda si tiene o no hongo, DUDA!!!!!!!!
						// el hongo afecta a toda la cepa o solo a unos determinados racimos?
						// es mas, se podria decir que afecta a parte de un racimo?



	void getTemperatura();
	void getLluvia();
	void humedad();
	void getTipoTierra();

	void hongo();

	void calcularCrecimientoUva();
	void calcularCrecimientoRacimo();
	void calcularCrecimeintoHoja();

	void calcularRacimoAprobechable(); // es la uva que se aprovecha para despues de a ver tenido o no hongo.
									//aunque creo que sera mejor llamarla calcularRacimoAprobechable






Hongo:


	//son atributos que comparte con el mundo
	int temperatura;
	float lluvia; //cantidad de lluvia  que cae 
	float humedad; 
	Strig tipoTierra; // tipo de tierra que es determiante en la produccion y calidad del vino
					  // por si queremos meter tambien este tipo de variable.
	int phTierra;


	//Atributos propios del hongo
	float velocidadViento;
	float ccecimiento;
	float expansion();



	void getTemperatura();
	void getLluvia();
	void humedad();
	void getTipoTierra();


	void calcularExpansion();	//se tendra en cuenta la velocidad del viento
	void calcularCrecimientoHongo();//Con las condiciones climaticas que hay en el momento
									// calculo cuanto podria llegar a crecer.